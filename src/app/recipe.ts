export class Recipe {
  id: number;
  name: string;
  // author: string;
  // month: string;
  // year: number;
  // location: string;
  // source: string; // will eventually be an object
  // ethnicity: string; // will eventually be an object
  // course: string; // will eventually be an object
  // categories: string; // display as csv in search results; eventually a list of object
  // comments: string;
  // rating: string;
}
